/*import 'package:shelf_router/shelf_router.dart';
import 'auth.dart';

final router = Router();

router.get('/auth/<token>', (Request request, String token) {
  return Response.ok(verifyToken(token) ? 'Token gültig' : 'Token ungültig');
})*/
